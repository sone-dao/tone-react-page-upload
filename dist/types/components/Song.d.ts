import { ReleaseSong } from '../types';
type ReleaseSongProps = {
    index: number;
    data: ReleaseSong;
};
export default function Song({ index, data }: ReleaseSongProps): import("react/jsx-runtime").JSX.Element;
export {};
